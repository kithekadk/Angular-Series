import { DynamicInsertionDirective } from './dynamic-insertion.directive';

describe('DynamicInsertionDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicInsertionDirective();
    expect(directive).toBeTruthy();
  });
});
